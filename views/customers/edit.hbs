<!DOCTYPE html>

<html lang="pt_BR">

<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title>Cliente | ElevaEmpilhadeiras</title>


  <link rel="icon" href="/images/logo-eleva.png" />

  <link rel="stylesheet" href="/css/main.css" />
  <link rel="stylesheet" href="/css/animations.css" />
  <link rel="stylesheet" href="/css/sidebar.css" />
  <link rel="stylesheet" href="/css/forms.css" />
  <link rel="stylesheet" href="/css/primary-button.css" />
  <link rel="stylesheet" href="/css/page-forms.css" />
  
  <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link href="https://fonts.googleapis.com/css2?family=Nunito:wght@300;400;600;700&display=swap" rel="stylesheet">
    <link rel="stylesheet" href="/css/home.css">
</head>

<body>
  <div id="page-creates">
    <aside class="animate-down sidebar">
      <img class="image-aside" src="/images/logo-eleva.png" alt="logo eleva" />

      <footer>
        <button>
          <a href="/"><img src="/images/arrow-back.svg" alt="Voltar" /></a>
        </button>
      </footer>
    </aside>

    <main class="animate-appear with-sidebar">
      <form action="/customers/update" method="POST">  <!-- Criando a rota de envio para o banco de dados -->
        <fieldset>
          <legend>Editar Cliente</legend>

          <input type="hidden" id="customerId" name="customer_id" value="{{customer.customer_id}}" required />
          <input type="hidden" id="addressId" name="address_id"  value="{{customer.address_id}}" required />

          <div class="input-block">
            <label for="customerName">Nome/Razão Social</label>
            <input id="customerName" name="name" maxlength="100" value="{{customer.name}}" required />
          </div>

          <div class="input-block">
            <label for="identify">CPF/CNPJ do Requisitante<span>Somente números</span></label>
            <input id="identify" name="identify" maxlength="15" value="{{customer.identify}}" required />
          </div>

          <div class="input-block">
            <label for="telephone">Telefone para Contato / Whatsapp</label>
            <input id="telephone" name="telephone" value="{{customer.telephone}}" required />
          </div>
        </fieldset>
        <fieldset>
          <legend>Endereço</legend>

          <div class="input-block">
            <label for="adress">Rua</label>
            <input id="adress" name="street" value="{{customer.street}}" required />
          </div>

          <div class="input-block input-row">
            <div class="input-block row">
                <label for="adressNumber">Número</label>
                <input type="number" id="number" name="number" value="{{customer.number}}" required />
            </div>
            <div class="input-block row">
                <label for="cep">CEP<span>Somente números</span></label>
                <input id="cep" maxlength="8" name="cep" value="{{customer.cep}}" required />
            </div>
          </div>

          <div class="input-block">
            <label for="adressComplement">Complemento</label>
            <input id="adressComplement" name="complement" value="{{customer.complement}}" required />
          </div>
        
          {{#if alertmessage}}
            <h4 class="alert-message">{{alertmessage}}</h4>
          {{else if successmessage}}
            <h4 class="success-message">{{successmessage}}</h4>
          {{/if}}

          <button type="submit" class="primary-button">Editar</button>

        </fieldset>
      </form>
    </main>
  </div>
</body>

</html>